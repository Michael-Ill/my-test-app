import React from "react";

const Header = () => {
    return (
        <div className="headerContainer">
            <h1 className="headerTitle text-center"> Someone's NHL API Page </h1>
        </div>
    );
};

export default Header;